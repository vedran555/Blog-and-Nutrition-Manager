import { Item } from './services/config.service';
import { AuthService } from './services/auth.service';


export const navbarItems = [
    { name: 'Dashboard', route: '/dashboard', show: true },
    { name: 'Profile', route: '/profile', show: true },
    { name: 'Blog', route: '/blog', show: true },
    { name: 'Login', route: '/login', show: true },
    { name: 'Register', route: '/register', show: true },
];